spec:
  name: openlinks
  static_sites:
    - name: openlinks
      github:
        repo: your-username/openlinks
        branch: main
        deploy_on_push: true
      build_command: npm install && npm run build
      output_dir: dist
      environment_slug: node-js
      routes:
        - path: /
